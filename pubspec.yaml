name: poker_game
description: Poker game

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  flutter_redux: ^0.5.3
  flutter_redux_navigation: ^0.4.1

dev_dependencies:
  pedantic: ^1.8.0+1
  test: ^1.6.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
